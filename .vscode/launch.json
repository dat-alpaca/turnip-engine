{
    "version": "0.2.0",
    "configurations": [
    
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/turnip-sandbox/turnip-sandbox",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}